open Base;; (* opening the Base library *)

(* ;; instructs the toplevel that it should evaluate the expression *)


3 + 4;; (* summing 3 + 4 *)

3.5 +. 6.;; (* float point summation *)

30_000_000 / 30_000;; (* conveniet form of writing long ints *)

(* let binding example *)

let x = 4 + 4;;

let y = x + x;;

let z = 2 * x;;
